FROM rust:1.90-alpine AS builder

RUN apk add --no-cache musl-dev gcc

WORKDIR /usr/src/app
COPY . .

RUN cargo build --release

FROM alpine:3.19
WORKDIR /app

COPY --from=builder /usr/src/app/target/release/plexus .

CMD ["./plexus"]
